<Page
    x:Class="EncodeConverter.FilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EncodeConverter"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:text="using:System.Text"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="EncodingTemplate" x:DataType="text:EncodingInfo">
            <TextBlock>
                <Run Text="{x:Bind DisplayName}" />
                <Run Foreground="Gray" Text="{x:Bind CodePage}" />
            </TextBlock>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Padding="20"
            Orientation="Vertical">
            <TextBlock Text="{x:Bind _vm.Path}" />
            <TextBlock Text="{x:Bind _vm.Name}" />
            <TextBlock Text="{x:Bind _vm.Content}" />
        </StackPanel>
        <StackPanel
            Grid.Row="0"
            Grid.Column="1"
            Padding="20"
            Orientation="Vertical">
            <ComboBox
                MinWidth="200"
                Header="原编码"
                ItemTemplate="{StaticResource EncodingTemplate}"
                ItemsSource="{x:Bind _vm.Encodings}"
                SelectedValue="{x:Bind _vm.SourceEncoding, Mode=TwoWay}"
                SelectionChanged="Selector_OnSelectionChanged" />
            <ComboBox
                MinWidth="200"
                Header="新编码"
                ItemTemplate="{StaticResource EncodingTemplate}"
                ItemsSource="{x:Bind _vm.Encodings}"
                SelectedValue="{x:Bind _vm.DestinationEncoding, Mode=TwoWay}"
                SelectionChanged="Selector2_OnSelectionChanged" />
        </StackPanel>
        <ListView
            x:Name="PredictListView"
            Grid.Row="1"
            Grid.Column="0"
            Header="预测编码"
            ItemsSource="{x:Bind _vm.DetectionDetails}"
            SelectionChanged="ListViewBase_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="text:EncodingInfo">
                    <local:EncodeResultItem
                        Model="{x:Bind}"
                        ReEncode="False"
                        RequestViewModel="EncodeResultItem_OnRequestViewModel" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView
            x:Name="PreviewListView"
            Grid.Row="1"
            Grid.Column="1"
            Header="结果预览"
            ItemsSource="{x:Bind _vm.Encodings}"
            SelectionChanged="ListViewBase2_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="text:EncodingInfo">
                    <local:EncodeResultItem
                        Model="{x:Bind}"
                        ReEncode="True"
                        RequestViewModel="EncodeResultItem_OnRequestViewModel" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
