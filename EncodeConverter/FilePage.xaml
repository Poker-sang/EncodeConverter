<Page
    x:Class="EncodeConverter.FilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:EncodeConverter"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:text="using:System.Text"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="EncodingTemplate" x:DataType="text:EncodingInfo">
            <TextBlock>
                <Run Text="{x:Bind DisplayName}" />
                <Run Foreground="Gray" Text="{x:Bind CodePage}" />
            </TextBlock>
        </DataTemplate>
    </Page.Resources>

    <Grid Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Padding="20"
            Orientation="Vertical">
            <TextBlock Text="{x:Bind _vm.Path}" />
            <TextBlock Text="{x:Bind _vm.Name}" />
            <TextBlock Text="{x:Bind _vm.ContentPreview}" />
        </StackPanel>
        <StackPanel
            Grid.Row="0"
            Grid.Column="1"
            Padding="20"
            Orientation="Vertical">
            <ComboBox
                Header="原编码"
                ItemTemplate="{StaticResource EncodingTemplate}"
                ItemsSource="{x:Bind _vm.Encodings}"
                SelectedValue="{x:Bind _vm.SourceEncoding, Mode=TwoWay}"
                SelectionChanged="Selector_OnSelectionChanged" />
            <ComboBox
                Header="新编码"
                ItemTemplate="{StaticResource EncodingTemplate}"
                ItemsSource="{x:Bind _vm.Encodings}"
                SelectedValue="{x:Bind _vm.DestinationEncoding, Mode=TwoWay}"
                SelectionChanged="Selector2_OnSelectionChanged" />
        </StackPanel>
        <ListView
            x:Name="PredictListView"
            Grid.Row="1"
            Grid.Column="0"
            Header="预测编码"
            IsItemClickEnabled="True"
            ItemClick="ListViewBase_OnItemClick"
            ItemsSource="{x:Bind _vm.DetectionDetails}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="text:EncodingInfo">
                    <StackPanel Padding="10" Spacing="5">
                        <TextBlock>
                            <Run Text="{x:Bind DisplayName}" />
                            <Run Foreground="Gray" Text="{x:Bind CodePage}" />
                        </TextBlock>
                        <TextBlock>
                            <Run Text="{x:Bind local:FilePageViewModel.GetPreviewSourceString(CodePage)}" />
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ListView
            x:Name="PreviewListView"
            Grid.Row="1"
            Grid.Column="1"
            Header="结果预览"
            IsItemClickEnabled="True"
            ItemClick="ListViewBase2_OnItemClick"
            ItemsSource="{x:Bind _vm.UpdatableEncodings, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="text:EncodingInfo">
                    <StackPanel Padding="10" Spacing="5">
                        <TextBlock>
                            <Run Text="{x:Bind DisplayName}" />
                            <Run Foreground="Gray" Text="{x:Bind CodePage}" />
                        </TextBlock>
                        <TextBlock>
                            <Run Text="{x:Bind local:FilePageViewModel.GetPreviewDestinationString(CodePage)}" />
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
